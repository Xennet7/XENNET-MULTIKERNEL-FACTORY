name: Script Tester

permissions:
  contents: write
  actions: write

on:
  workflow_dispatch:
    inputs:
      kernelsu_variant:
        description: Select KernelSU
        required: true
        type: choice
        options:
          - Official
          - Next
          - MKSU
          - SukiSU
      kernelsu_branch:
        description: Select the ksu branch
        required: false
        type: string
      susfs_commit:
        description: SUSFS Commit
        required: false
        type: string
      kernelsu_commit:
        description: KSU Commit
        required: false
        type: string
      version:
        description: Custom version name (e.g. 6.6.77-android15-8<custom version name>/leave blank to use the default version number)
        required: false
        type: string
      use_zram:
        description: 'Do you want to enable adding more ZRAM algorithms?'
        required: true
        type: boolean
        default: true
      use_bbg:
        description: 'Do you want to enable the BBG write-blocking patch? (Disable it if it reports an error)'
        required: true
        type: boolean
        default: true
      use_kpm:
        description: 'Do you want the KPM function enabled?'
        required: true
        type: boolean
        default: true
      supp_op:
        description: 'Do you want to enable SUPP_OP patches?'
        required: false
        type: boolean
        default: false
      onlyAk3:
        description: 'Only build AK3 artifacts?'
        required: false
        type: boolean
        default: false
      build_time:
        description: Set kernel build time (format see repository description)
        required: false
        type: string
      get_manager:
        description: Get the latest KSU Manager and SUSFS modules?
        required: true
        type: boolean
        default: true
      make_release:
        description: Make GitHub release (for tested builds)
        required: true
        type: boolean
        default: false

jobs:
  test-script-main:
    uses: ./.github/workflows/test-main.yml
    secrets: inherit
    with:
      make_release: false
      kernelsu_variant: ${{ inputs.kernelsu_variant }}
      kernelsu_branch: ${{ inputs.kernelsu_branch }}
      susfs_commit: ${{ inputs.susfs_commit }}
      kernelsu_commit: ${{ inputs.kernelsu_commit }}
      version: ${{ inputs.version }}
      use_zram: ${{ inputs.use_zram }}
      use_bbg: ${{ inputs.use_bbg }}
      use_kpm: ${{ inputs.use_kpm }}
      supp_op: ${{ inputs.supp_op }}
      onlyAk3: ${{ inputs.onlyAk3 }}
      build_time: ${{ inputs.build_time }}

  test-manager:
    if: ${{ inputs.get_manager }}
    uses: ./.github/workflows/test-manager.yml
    secrets: inherit

  test-releasesuki:
    if: ${{ inputs.kernelsu_variant == 'SukiSU' }}
    uses: ./.github/workflows/testksu.yml
    secrets: inherit
  test-releaseksu:
    if: ${{ inputs.kernelsu_variant == 'Official' }}
    uses: ./.github/workflows/testksu.yml
    secrets: inherit

  test-releasemksu:
    if: ${{ inputs.kernelsu_variant == 'MKSU' }}
    uses: ./.github/workflows/testmksu.yml
    secrets: inherit

  test-releasenext:
    if: ${{ inputs.kernelsu_variant == 'Next' }}
    uses: ./.github/workflows/testnext.yml
    secrets: inherit
  
